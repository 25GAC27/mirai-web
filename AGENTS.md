# Project Instructions for AI Agent

## Project Overview

本プロジェクトは、伝統みらい研究センターと呼ばれる柿右衛門様式の陶器を中心とした九州各地の伝統工芸品の研究を行っているセンターの Web サイトである。

-   **目的**: 伝統工芸品に関する研究成果、工芸品の紹介、およびイベント情報を、誰でも容易に Github Pages 上で、Markdown ファイルを修正、追加し、コミットを行い、GitHub Actions によって自動的にビルドして、公開すること。イメージとしては、Pukiwiki システムのサーバーがないものである。(常に静的なページ)
-   **アーキテクチャ**: Atomic Design の原則に従う。
-   **ターゲット環境**: HTML, CSS, JavaScript (ES2024), Markdown。

---

## Content Structure (コンテンツ構造)

以下のディレクトリ構造と命名規則に従って、全てのコンテンツを Markdown ファイルとして格納すること。エージェントは、これらのパスからビルド時にデータを読み込み、静的 HTML ファイルを生成する仕組みを構築すること。

project-root/
│
├── index.html                   # TopPage（トップページ、メインビジュアル）
│
├── css/                         # スタイルシート格納フォルダ
│   ├── style.css
│   └── reset.css
│
├── js/                          # JavaScript格納フォルダ
│   └── main.js
│
├── images/                      # 画像フォルダ
│   ├── common/                  # 共通画像（ロゴなど）
│   ├── top/                     # トップページ用画像
│   ├── kakiemon/                # 柿右衛門研究部門用画像
│   └── regional/                # 地域産業研究部門用画像
│
├── news/                        # お知らせ 
│   ├── index.html               # お知らせ一覧
│   └── article.html             # お知らせ詳細ページテンプレート
│
├── about/                       # センターについて 
│   ├── index.html               # センター概要・歴史・設立背景・組織図 
│   ├── members.html             # 委員一覧 
│   └── archive.html             # 情報・陶芸研究センターアーカイブ 
│
├── kakiemon/                    # 柿右衛門研究部門 
│   ├── index.html               # 研究部門トップ
│   ├── about.html               # 柿右衛門とは？（14代・15代・歴史）
│   ├── style.html               # 柿右衛門様式・文様・素材・薪について 
│   ├── works.html               # 柿右衛門作品（九産大所蔵含む）
│   ├── research.html            # 研究成果（要約・リンク）
│   └── connection.html          # その他のつながり（有田・マイセン・窯について）
│
├── regional/                    # 地域産業研究部門 
│   ├── index.html               # 研究部門トップ 
│   ├── crafts.html              # 伝統工芸とは・工芸品一覧（九州）
│   ├── research.html            # 研究成果（論文・ポスター）
│   └── connection.html          # 他地域とのつながり（うなぎの寝床・匠ギャラリー等）
│
├── links/                       # 外部リンク集 
│   └── index.html               # リンク一覧 
│
└── access/                      # アクセス・お問い合わせ 
    └── index.html

---

## 機能 (Features)

以下の機能を実装すること。全ての情報は Markdown ファイルから読み込まれるものとする。

### 1. 研究成果一覧と詳細表示

-   `/research` パスで、全ての研究記事の一覧を表示する。
-   記事のタイトル、公開日、概要（Markdown の先頭数行）を表示する。
-   記事をクリックすると、`/research/[slug]` パスで詳細を表示する。

### 2. 工芸品紹介ページ

-   `/crafts` パスで、工芸品（例: 柿右衛門様式、博多織など）ごとの概要ページを表示する。
-   各工芸品の紹介文は Markdown ファイルから読み込む。

### 3. お知らせ/イベント情報

-   `/news` パスで、最新のお知らせ（イベント告知、休館情報など）を時系列で表示する。
-   過去のお知らせも `/news/[slug]` で個別に閲覧可能とする。

### 4. 検索機能

-   Web サイト全体を対象とした全文検索機能を実装する。
-	検索バーをクリックすると Apple のサイトのように、背景が半分白で覆われ、その下の背景が影で覆われ、検索結果をリアルタイムで検索バーの下に表示されるようにすること。

---

## Design System Guidelines

### Color Palette & Usage

各カテゴリに対応する色は以下のルールに従って適用すること。また、サイト全体の色はモノクロにしてシンプルな配色を心がけること。

| Semantic Name | Color Label | Hex | Usage Rule |
| :--- | :--- | :--- | :--- |
| **Kakiemon** | Red | #D15B1D | 柿右衛門に関するコンテンツに使用  |
| **Traditional Crafts** | Blue | #1982C4 | 伝統工芸に関するコンテンツに使用 |
| **Center Info** | Green | #27A38D | センターに関するコンテンツに使用 |
| **Visuals** | Yellow | #B9A722 | ビジュアルに関するコンテンツに使用 |
| **External Links** | Purple | #6A4C93 | 外部リンクに使用 |

### Design

デザインは Wikipedia を参考にし、シンプルでカスタマイズの簡単さを重視する。その中で、Apple のデザインを意識したデザインにすること。

---

## Code Style and Conventions (コードスタイルと規約)

-   **言語**: 本プロジェクトは、ビルドスクリプトとランタイムの全てで「HTML」「CSS」「JavaScript」「Markdown」のみで構成される。これら以外の言語の使用は認めない。

-   **HTML**:
    -   **Target**: HTML Living Standard (最新のセマンティック要素を使用すること)。
-   **CSS**:
    -   **Target**: CSS 最新仕様 (Flexbox, Grid, Custom Properties を積極的に活用すること)。
-   **JavaScript**:
    -   **Target**: ECMAScript 2024 (ES2024)。
    -   関数は基本的に「アロー関数」を用いて実装すること。
    -   ライブラリは「markdown-it」のみ使用を認め、ビルドプロセスおよびランタイムでの外部依存はこれを唯一とする。
-   **Markdown**:

    -   **Target**: **CommonMark** または **GFM (GitHub Flavored Markdown)** に準拠すること。

-   **命名規則**:
    -   変数/関数名: `camelCase`
-   **コミットメッセージ**: Conventional Commits 規約に従うこと。（例: `feat(ui): Add TodoItem component`）

## Markdown ファイルの書き方（ルール）

Markdown ファイルは、すべてのコンテンツ情報を格納する唯一のソースであり、**必ず**「YAML Front Matter」（メタデータ）と「本文」の2部構成で作成する。

### 1. YAML Front Matter（メタデータ）の記述

ファイルの一番最初に、記事のタイトル、日付、カテゴリなどの情報を`---`（ハイフン3つ）で囲まれたブロックの中に定義します。このメタデータは、情報の表示、一覧化、検索機能のために不可欠である。

| 項目 | 役割 |
| :--- | :--- |
| **必須メタデータ** | 記事を公開するために最低限必要な情報。 |
| **分類・検索用メタデータ** | フィルタリングや検索を助ける情報。（例: `style`, `era`, `motifs`など） |
| **画像パス** | 記事内で使用する画像のファイル場所。 |

> **📌 サンプル例（工芸品紹介のメタデータ）**
>
> ```yaml
> ---
> title: "色絵竹文皿" # 作品の名称
> id: "KAE-001" # アーカイブ内のユニークID
> date: 1700 # 制作年代（西暦）
> layout: work # このメタデータに対応する表示テンプレート
> style: "柿右衛門様式" # 識別用スタイル
> era: "延宝〜元禄期" # 制作時期（細分化）
> category: "皿" # 器種（皿、鉢、壺など）
> dimensions: "高 4.0cm / 径 21.5cm" # 寸法情報
> owner: "個人蔵" # 所蔵先
> image_paths: # 画像パスのリスト
>   - /assets/images/kakiemon/KAE-001_front.webp
> ---
> ```
>
> **Front Matter の直後に、記事の本文（マークダウン）を記述すること。**

### 2. Markdown 記法

Front Matter の直後から、以下の CommonMark/GFM 記法を使って本文を記述する。

| やりたいこと | 書き方 | Web サイトでの見え方 |
| :--- | :--- | :--- |
| **大きな見出し**を作る | `# これは見出し` | 見出し１（一番大きい） |
| **中くらいの見出し**を作る | `## これはサブ見出し` | 見出し２ |
| **太字**にする | `**太字のテキスト**` | **太字のテキスト** |
| **リスト**を作る | `* リストの項目1` | ・リストの項目 1 |
| **リンク**を貼る | `[リンクの文字](URLアドレス)` | リンクの文字 |

---

## Gotchas (注意点)

-   本プロジェクトの最重要な点は「**誰でも簡単にマークダウンファイルを追加するだけで情報の追加・更新を行うことが可能**」な点である。この条件を満たさない仕様は認めない。
-   ビルド時のパフォーマンス（ビルド速度）と、ランタイムでの初期ロード速度（特に Lighthouse スコア）を最優先すること。
-   フレームワークや外部 SSG ツールの使用は禁止とし、Node.js のファイルシステム操作と`markdown-it`のみを用いて静的 HTML を生成するカスタムスクリプトを作成すること。
